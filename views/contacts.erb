<h2>Add a contact : </h2>

<h2>There are <%= @contact_collection.length %> contacts </h2>

<% @contact_collection.each do |contact| %>
  <li> <%= contact.full_name %> <%= contact.email %> <%= contact.note%> </li>
  <% end %>
